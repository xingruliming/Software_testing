# 2026-09-10

## VGGT 输入/输出目录整理

- 工作目录：`E:\办公\研一\1软件实践\Software_testing\vggt_test`（VGGT 官方仓库副本）
- 需求：把 VGGT 的输入、输出文件夹单独分列出来
- 做法（用户选择「新建两个独立文件夹」）：
  - 新建 `vggt_test/input/`：从原 `examples/` **复制**（不是移动，保留原目录以免破坏 demo 脚本默认路径）
    - `input/<场景>/images/`：kitchen(25)、llff_fern(20)、llff_flower(25)、room(8)、single_cartoon(1)、single_oil_painting(1)，共 68 张图（另含 png 72）
    - `input/videos/`：8 个 mp4
    - 共 88 个文件、约 63MB
  - 新建 `vggt_test/output/`：空的输出目录，含 `.gitkeep` + `README.md`
  - 两个目录各写了一份 `README.md` 说明用途和对应脚本参数

## VGGT 仓库关键信息（备忘）

- 三个 demo 脚本：
  - `demo_viser.py` → `--image_folder`（默认 `examples/kitchen/images/`），viser 可视化服务，默认端口 8080
  - `demo_colmap.py` → `--scene_dir`（内部找 `images/` 子目录），产出 COLMAP 格式 `sparse/{cameras,images,points3D}.bin` + `points.ply`
  - `demo_gradio.py` → 网页上传，运行后在根目录新建 `input_images_<时间戳>/`，内含 `predictions.npz` 和 `glbscene_*.glb`
- 注意：`demo_gradio.py` 的工作目录兼输出目录命名前缀是 `input_images_`，实际属于输出产物

## 【重做】目录位置改为 Software_testing 根目录下

- 用户要求重做，位置改为 `E:\办公\研一\1软件实践\Software_testing\` 下（不是 vggt_test 里面）
- **工作区已变化**：`vggt_test/` 已不存在，现在仓库目录是 `vggt-main/`（更完整的版本）
  - 多出：`demo_gradio_cn.py`（中文界面版）、`export_ply.py`（npz→ply）、`skyseg.onnx`（176MB 天空分割模型）、`main.py`、`training/`、`.idea/`、`.gradio/`
  - `main.py` 里权重路径硬编码为 `E:\0_work\vggt\model.pt`
  - 根目录 `Software_testing/` 本身是一个 git 仓库
- 最终建成：`Software_testing/vggt_input/`（复制自 `vggt-main/examples/`，88 文件 63MB）+ `Software_testing/vggt_output/`（空，含 `.gitkeep` + README）
- 命名采用 `vggt_input` / `vggt_output` 前缀式，避免与后续其他实验目录混淆

## 重要坑位（测试时注意）

- `demo_colmap.py` **就地输出**：直接往 `{scene_dir}/` 写 `sparse/` 和 `points.ply`，会污染输入目录 → 建议先复制场景副本到 `vggt_output/` 再跑
- `demo_viser.py --mask_sky` 会在 `{image_folder}` 旁生成 `<image_folder>_sky_masks/`
- `demo_gradio.py` / `demo_gradio_cn.py` 在**当前工作目录**建 `input_images_<时间戳>/`（是输出），内含 `predictions.npz`、`predictions.ply`、`glbscene_*.glb`
- `vggt_input/`（63MB）当前是 untracked，根目录无 `.gitignore`，`git add .` 会把它一起提交

## 【改造】demo_gradio_cn.py 增加产物自动备份

- 需求：每次产生输出后，把输出内容复制一份到 output 文件夹
- 用户确认：目标位置 = `Software_testing/vggt_output`（选 **`vggt_output/gradio/`** 子目录）；范围 = **只复制模型产物**（不复制上传图/抽帧图）
- 改动（均在 `vggt-main/demo_gradio_cn.py`）：
  1. 顶部新增「0) 产物备份配置」段：`SCRIPT_DIR` / `OUTPUT_BACKUP_DIR`（= `ScriptDir/../vggt_output/gradio`，用相对解析不写死绝对路径）/ `BACKUP_FILE_PATTERNS = ["predictions.npz", "glbscene_*.glb"]`
  2. 新增 `backup_outputs(target_dir, files=None)` 函数：`files=None` 时按 patterns 通配收集；显式传入时只备份指定文件；备份到 `OUTPUT_BACKUP_DIR/<去掉 input_images_ 前缀的时间戳>/`；`copy2` 复制（不移动）；异常只打印不抛，不打断主流程；无可备份文件返回 None
  3. `gradio_demo()` 里 glb 导出后调用 `backup_outputs(target_dir)`，并在日志加「｜ 产物已备份」
  4. `update_visualization()` 中仅在**新生成** glb 的分支里调用 `backup_outputs(target_dir, files=[glbfile])`，避免每次滑条变动都全量重拷
- 验证：`py_compile` 通过；另写 AST 抽取真实函数体做了 4 个用例测试（自动收集 / 显式指定增量备份 / 非法 target_dir / 空目录），全部通过，测试脚本已删除
- 同步更新了 `vggt_output/README.md` 说明该自动备份行为；预建了 `vggt_output/gradio/`
- `demo_gradio.py`（英文版）**未改造**，仍是原行为

## 【改动】模型权重路径切换到统一位置

- `demo_gradio_cn.py` 第 33 行权重路径：
  `E:\0_work\suanfa\vggt\vggt-main\model.pt` → **`E:\办公\研一\1软件实践\model.pt`**
- 该文件实际存在，约 5.02 GB（2026-06-07）
- 已 `py_compile` 校验通过

## 【修 Bug】demo_gradio_cn.py 预测模式中英文不一致

- 报错现象：点示例行时 Gradio 抛
  `Error: "Value: 'Depthmap and Camera Branch' is not in the list of choices: ['深度图与相机分支','点云图分支']"`
- **根因 1（崩溃）**：`examples` 列表里 prediction_mode 用英文，Radio 的 choices 是中文 → gradio `radio.py:preprocess` 校验失败
- **根因 2（静默错分支）**：`visual_util.py:68` 用 `if "Pointmap" in prediction_mode` 选分支。中文 `"点云图分支"` 不含 `Pointmap` → **选点云图分支实际跑成深度图分支**（不报错但结果错）
- 修复（4 处，均在 `demo_gradio_cn.py`）：
  1. 新增 `PREDICTION_MODE_MAP` + `normalize_prediction_mode()`，中文→英文规范值（`深度图与相机分支`→`Depthmap and Camera Branch`，`点云图分支`→`Pointmap Branch`），空值兜底为 Depthmap
  2. `gradio_demo()` 入口归一化
  3. `update_visualization()` 入口归一化（保证 glb 文件名/缓存一致）
  4. `examples` 列表 7 行的 prediction_mode 全改成中文 `"深度图与相机分支"`
- 选择在脚本内归一化而**不改** `visual_util.py`，避免影响英文版 demo
- 验证：`py_compile` 通过；7 个用例（含 None/空串兜底）确认映射后分支正确，测试脚本已删
- 对照确认：英文版 `demo_gradio.py` choices 与 examples 都是英文，**无此问题**
- 附带说明：报错里的 `ConnectionResetError: [WinError 10054] ... _call_connection_lost` 是 Windows 上 asyncio Proactor 的已知无害告警，与本次故障无关

## 【修复】share=True 导致无法创建公网分享链接

- 报错：`login to server failed: connection write timeout` + `Could not create share link. Please check your internet connection or status page: https://status.gradio.app`
- 根因：`launch(share=True)` 需要连 `gradio.app` 的中继服务器（frpc）生成公网链接，**国内网络连不通** → 超时
- 修复（`demo_gradio_cn.py`，2 处）：
  1. 新增启动参数常量段：`ENABLE_SHARE = False` / `SERVER_NAME = "127.0.0.1"` / `SERVER_PORT = 7860`
  2. `launch(...)` 显式传参：`server_name` / `server_port` / `share=ENABLE_SHARE` / `inbrowser=True` / `show_error=True`
- 需要公网链接时的备选：改 `ENABLE_SHARE=True`（需能访问 gradio.app），或用 ngrok / cloudflared 做内网穿透；局域网访问把 `SERVER_NAME` 改 `"0.0.0.0"`
- 英文版 `demo_gradio.py` 第 727 行仍是 `share=True`，**未改**（如需一并改可再说）
- 已 `py_compile` 校验通过

## 【新增功能】demo_gradio_cn.py 可视化页面加深度图展示框

- 需求：可视化页面增加一个深度图展示框
- 新建 `save_depth_images(predictions, target_dir, max_frames=DEPTH_MAX_FRAMES)`：
  - 读 `predictions["depth"]`（(S,H,W,1) / (S,H,W) / (H,W) 三种形状都兼容）
  - **2%~98% 分位数归一化**（抗离群值），常量图/NaN/Inf 都有兜底
  - `cv2.applyColorMap` 伪彩色渲染，存到 `target_dir/depth/{i:06d}.png`
  - 返回 `[(路径, "帧 N"), ...]` 直接喂 Gallery
- 新增常量：`DEPTH_COLORMAP = "INFERNO"`、`DEPTH_MAX_FRAMES = 64`
- UI：在 3D 查看器下方新增 `gr.Gallery` 深度图展示框（columns=4, height=260px），并加入 `ClearButton` 的清理列表
- 接线改动（gradio 回调返回值/输出个数，全部核对过配对）：
  - `gradio_demo` 返回 4 值（新增 depth_images）← submit_btn outputs 4
  - `example_pipeline` 返回 6 值 ← gr.Examples outputs 6
  - `update_gallery_on_upload` 返回 5 值（新增清空深度图）← 两个 .change outputs 5
  - `update_visualization` 保持 2 值不变（深度图与可视化参数无关，不重复刷新）
  - 副作用修复：`gradio_demo` 的提前返回原本就是 4 值但只有 3 个 outputs（潜在不匹配 bug），现在正好对上
- 验证：`py_compile` 通过；用 AST 抽取真实函数体跑 7 个用例（标准 4D / 3D / 单帧 / 常量深度 / NaN+Inf / max_frames 截断 / 无 depth 字段），在 `D:\Anaconda3\envs\Pytorch_Vggt`（cv2 4.13.0 + numpy 2.2.6）下全部通过，测试脚本已删
- 注意：深度图 PNG **未**加入 `BACKUP_FILE_PATTERNS`（尊重用户此前"只备份 npz+glb"的选择）；如需备份，加一条 `"depth/*.png"` 即可

## 【修复】端口 7860 被占用报 OSError

- 报错：`OSError: Cannot find empty port in range: 7860-7860`
- **根因：僵尸进程**。之前运行过的 gradio 实例没退干净：
  - PID 12576 python.exe ~1.4GB 占 7860
  - PID 32260 python.exe ~2.0GB 占 7861（后来又跑了一次）
  - 合计约 3.4GB 内存被占
- 修复（`demo_gradio_cn.py`）：
  1. 新增 `import socket` + `is_port_free(port, host)`：**故意不设 SO_REUSEADDR**（Windows 上设了会导致已占用端口也能 bind，误判为空闲）
  2. `launch` 前探测 `SERVER_PORT`：空闲就用，占用则传 `server_port=None` 交给 Gradio 自带的端口轮询（从 INITIAL_PORT_VALUE 起逐个试到真正启动成功）
  3. `SERVER_PORT` 填 `None` 表示完全自动
- 验证：`is_port_free(7860)=False`、`is_port_free(7862)=True`，错误码 10048(WSAEADDRINUSE)，判断正确
- 参考：gradio 的 `http_server.py:start_server` 里 `server_ports = [server_port] if server_port is not None else range(INITIAL_PORT_VALUE, INITIAL_PORT_VALUE + TRY_NUM_PORTS)`

## 【根治】Windows asyncio ConnectionResetError 刷屏

- 现象（用户反复遇到）：浏览器刷新/关闭页面就打印
  `Exception in callback _ProactorBasePipeTransport._call_connection_lost(None)`
  `ConnectionResetError: [WinError 10054] 远程主机强迫关闭了一个现有的连接。`
- 根因：环境为 **Python 3.10.20**，`asyncio/proactor_events.py` **第 165 行**
  `_ProactorBasePipeTransport._call_connection_lost` 的 `finally` 块里调用
  `self._sock.shutdown(socket.SHUT_RDWR)` 抛错。因该回调由 `loop.call_soon` 调度，
  asyncio 视作未处理异常打印。CPython 已知问题 **gh-93823**
- **额外损害**（不只刷屏）：异常抛在 `finally` 中，导致后面的
  `_sock.close()`、`_sock = None`、`server._detach()`、`_called_connection_lost = True`
  **全部被跳过**，transport 处于半清理状态
- 修复：`demo_gradio_cn.py` 新增 `_patch_windows_asyncio_connection_reset()` 并在模块导入后立即调用
  - 仅 `sys.platform == "win32"` 时生效
  - 包一层 `_call_connection_lost`：捕获 `ConnectionResetError` 后**补完上述清理**并返回 None
  - 其他异常（如 RuntimeError）仍照原样抛出；补丁失败只打印提示不影响运行
- 验证（`D:\Anaconda3\envs\Pytorch_Vggt`）：用 AST 抽取真实补丁源码跑两组测试
  - 桩对象：确认原方法确实抛错且 `_called_connection_lost` 仍为 False（证明清理被跳过）；打补丁后不抛错、`_sock` 置空、`server._detach()` 被调用、`_called_connection_lost=True`；RuntimeError 仍正常抛出
  - 真实 ProactorEventLoop：正常 TCP 收发不受影响（收到 b'hello'），SO_LINGER 强制 RST 后事件循环捕获的 ConnectionResetError 为 0
