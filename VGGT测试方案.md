# VGGT 算法测试方案

| 项目 | 内容 |
|---|---|
| 被测对象 | VGGT（Visual Geometry Grounded Transformer），本地副本 `vggt-main/` |
| 被测功能 | 多视图 3D 重建：`demo_gradio_cn.py` 的 Reconstruct 流程，核心为 VGGT 单次前馈推理 |
| 测试目标 | ① 无真值条件下的几何自洽性与稳定性；② 有真值条件下的重建精度；③ 鲁棒性与资源开销 |
| 不在范围内 | 模型训练过程、论文指标复现（缺少训练数据与算力） |
| 配套工具 | `metrics.py`（无真值指标计算）、`vggt_output/`（结果归档） |
| 编写日期 | 2026-09-10 |

---

## 1. 被测系统的输出与坐标约定

> 这一节是所有指标计算的前提。约定已对着 `vggt/` 源码逐一核实，与论文/示例代码一致。

`predictions.npz` 中的字段（由 `demo_gradio_cn.py` 的 `run_model()` 落盘）：

| 字段 | 形状 | 含义 |
|---|---|---|
| `depth` | (S, H, W, 1) | 相机 z 向深度，非欧氏距离 |
| `depth_conf` | (S, H, W) | 深度置信度 |
| `world_points` | (S, H, W, 3) | **点图头**直接输出的世界坐标 |
| `world_points_conf` | (S, H, W) | 点图置信度 |
| `world_points_from_depth` | (S, H, W, 3) | 由 **深度 + 位姿反投影**算出的世界坐标 |
| `extrinsic` | (S, 3, 4) | OpenCV 约定，**world → cam**：`X_cam = R @ X_world + t` |
| `intrinsic` | (S, 3, 3) | **像素单位**，主点在图像中心 `(W/2, H/2)` |
| `images` | (S, 3, H, W) | 取值 [0,1]，已 `ToTensor`，**未做** mean/std 归一化 |

要点：

- **第一帧为参考系**，其位姿固定为单位阵。换第一帧 ⇒ 整场景坐标系随之改变，因此跨运行的**绝对**位姿不可直接比较，只能比**相对位姿**。
- 输入图像统一预处理为宽 518px、高按 14 对齐（`load_and_preprocess_images(mode="crop")`），`H/W` 与 `depth` 一致。
- 点图头与"深度+位姿"是**两套冗余输出**，论文实测后者反投影更准（ETH3D Chamfer 0.677 vs 0.709）—— 这正好构成一个天然的交叉验证点。

---

## 2. 指标体系

### 2.1 精度指标（需要真值 GT）

| 指标 | 定义 | 方向 | 对应任务 |
|---|---|---|---|
| **AUC@30** | 位姿误差阈值 30° 内的累积误差曲线下面积（官方主指标） | ↑ | 相机位姿 |
| RRA@15 / RTA@15 | 相对旋转/平移误差在 15° 内的正确率 | ↑ | 相机位姿 |
| mAA(30) | 平均角精度 | ↑ | 相机位姿 |
| AbsRel | `mean(|d̂−d|/d)` | ↓ | 深度 |
| RMSE / RMSE_log | 均方根误差（对数域） | ↓ | 深度 |
| δ1.03 / δ1.25 | `max(d̂/d, d/d̂) < 1.03 (1.25)` 的像素占比 | ↑ | 深度 |
| Accuracy | 预测点云到 GT 的平均距离 | ↓ | 点云/点图 |
| Completeness | GT 到预测点云的平均距离 | ↓ | 点云/点图 |
| **Overall Chamfer** | `(Accuracy + Completeness) / 2` | ↓ | 点云/点图 |
| F-score@τ | 在阈值 τ 内互匹配的点对占比 | ↑ | 点云/点图 |
| Normal Consistency | 法向一致性 | ↑ | 点云/点图 |
| AUC@5/10/20 | 两视图匹配精度 | ↑ | 图像匹配 |
| AJ / δ_avg^vis | 平均 Jaccard、可见点平均精度 | ↑ | 点跟踪 |

官方评测数据集：位姿用 RealEstate10K / CO3Dv2，深度用 DTU，点图用 ETH3D，匹配用 ScanNet-1500，跟踪用 TAP-Vid。速度基线（H100）：纯前馈 ≈ 0.2 s，加 BA ≈ 1.8 s。

### 2.2 几何自洽性指标（**无需真值**，本方案重点）

| 编号 | 指标 | 计算方式 | 方向 |
|---|---|---|---|
| **G1** | 点图头 vs 深度反投影 | 在置信度过滤后的像素上算 `‖world_points − world_points_from_depth‖₂`，同时报告除以场景包围盒对角线长度的**相对值** | ↓ |
| **G2** | 点图头 vs 相机头 | 把 `world_points` 用自身 `extrinsic` 投回相机，比较投影坐标与理想像素网格的**像素误差**，以及 z 值与 `depth` 的**相对误差** | ↓ |
| **G3** | 置信度分布 | `depth_conf` / `world_points_conf` 的均值、分位数、低于阈值的像素占比 | 参考 |
| **G4** | 多视图重投影光度一致性 | 用帧 i 的深度+位姿把像素投到帧 j，双线性采样帧 j 图像，算 L1 与 NCC；并记录有效投影率 | ↓ / ↑ |
| **G5** | 分段一致性（窗口一致性） | 对同一序列用**不同帧窗口**各跑一次，比较重叠帧之间的**相对位姿** | ↓ |

各指标能捕获的故障类型：

| 指标 | 能发现的问题 |
|---|---|
| G1 | 点图头与深度/相机头不一致（三个 head 解耦失效） |
| G2 | 外参方向约定错误、内外参标定漂移、点图头坐标错位 |
| G3 | 模型对某些场景/区域不确定，可用于定位退化区域 |
| G4 | 深度与位姿**联合**不一致、遮挡/视差问题、几何漂移 |
| G5 | 预测对上下文窗口的依赖程度（长序列稳定性） |

> ⚠️ **重要更正：环闭合检验对 VGGT 无效。**
> 直觉上"取 A→B→C 三帧，验证 `T_AB·T_BC·T_CA ≈ I`"像是漂移指标，但**推导后它对单次前馈输出恒等于单位阵**：
> 设 `extrinsic_i` 为 world→cam 的 `T_i`，则 `T_{A→B} = T_B T_A^{-1}`。
> 复合变换按施加顺序为 `T_{C→A} · T_{B→C} · T_{A→B} = (T_A T_C^{-1})(T_C T_B^{-1})(T_B T_A^{-1}) = I`，
> 相邻项**逐项抵消**，无论位姿多离谱都是单位阵（残差只有浮点误差 ~1e-7）。
> 原因是 VGGT 一帧一次前馈、所有位姿同源于一次预测，天然互相一致。
> 因此该项**必须用 G5 分段一致性替代**——通过改变输入窗口引入真正独立的两次预测，再比较重叠帧的相对位姿。

### 2.3 鲁棒性指标

| 维度 | 扰动方式 | 观测量 |
|---|---|---|
| 帧数 | N = 1 / 2 / 5 / 10 / 20 / 50 | G1–G4 随 N 的变化曲线 |
| 帧顺序 | 打乱输入顺序 | 重叠帧相对位姿的偏差（参照第一帧锚点变化） |
| 图像质量 | 高斯噪声 σ、JPEG 质量、运动模糊、亮度/对比度变换 | G2、G4 退化幅度 |
| 遮挡 | 随机遮挡 10% / 25% / 50% 面积 | G1、G4 退化幅度 |
| 分辨率 | 原图 1× / 0.5× / 0.25× | G2、G4 退化幅度 |
| 场景难度 | 低纹理、弱重叠、大视角变化、重复纹理 | G3 低置信占比、G4 有效率 |

### 2.4 效率与工程质量指标

| 指标 | 说明 | 方向 |
|---|---|---|
| 端到端推理耗时 | `run_model` 单次耗时（需首帧预热后取中位数） | ↓ |
| 峰值显存 | `torch.cuda.max_memory_allocated()` | ↓ |
| 吞吐 | 帧/秒；以及**帧数—显存**曲线（VGGT 最敏感的资源维度） | ↑ |
| 模型加载时间 | `torch.load` + `load_state_dict` 耗时 | ↓ |
| 确定性 | 固定随机种子下多次运行输出是否逐位一致 | 一致 |
| 稳定性 | 连续运行 10 次，成功率与显存是否单调增长（泄漏检测） | ↑ |
| 异常输入 | 0 张图 / 1 张图 / 损坏文件 / 非图像文件 / 超大分辨率 | 不崩溃且有明确报错 |
| 产物完整性 | `predictions.npz` 可读、`glbscene_*.glb` 可加载、深度图生成数 = 帧数 | 通过 |

---

## 3. 测试用例设计

| 编号 | 场景 | 配置 | 观测指标 | 预期 |
|---|---|---|---|---|
| TC-01 | `room` | 8 帧，默认参数，seed 固定 | G1–G4 + 耗时 + 显存 | 基线，记录四指标数值 |
| TC-02 | `llff_fern` | 20 帧 | G1–G4 | 帧数增加后各指标退化幅度 |
| TC-03 | `kitchen` | 25 帧 | G1–G4 | 同上，更长序列 |
| TC-04 | `single_cartoon` | 1 帧 | G1–G3 | G4 应报"不可用" |
| TC-05 | `room` | N = 1/2/5/8 逐档 | G1–G4 | 画帧数—指标曲线 |
| TC-06 | `kitchen` | 顺序打乱 3 种 | G5 + G2 | 相对位姿偏差量级 |
| TC-07 | `room` | 高斯噪声 σ = 0/5/10/20 | G2、G4 | 单调退化 |
| TC-08 | `room` | 随机遮挡 10%/25%/50% | G1、G4 | 单调退化，低置信占比上升 |
| TC-09 | `room` | 分辨率 1×/0.5×/0.25× | G2、G4 | 单调退化 |
| TC-10 | `room` | 重复运行 10 次，同 seed | G1–G4 逐次一致 | 可复现 |
| TC-11 | 异常输入 | 空目录 / 1 张图 / 损坏 jpg | 是否崩溃、报错信息 | 优雅降级 |
| TC-12 | 长序列 | 50 帧 | 峰值显存、是否 OOM | 记录显存上限 |

---

## 4. 环境与数据准备

**运行环境（实测）**

| 项 | 值 |
|---|---|
| Python | 3.10.20（Anaconda 环境 `Pytorch_Vggt`） |
| 关键依赖 | torch（CUDA）、opencv-python 4.13.0、numpy 2.2.6、gradio |
| 模型权重 | `E:\办公\研一\1软件实践\model.pt`（约 5.02 GB） |
| 输入数据 | `vggt_input/`（6 个场景 + 8 段视频） |
| 结果归档 | `vggt_output/`（demo 会自动备份产物到 `vggt_output/gradio/<时间戳>/`） |

**真值数据集（做精度指标时才需要）**：CO3Dv2、RealEstate10K（位姿）、DTU（深度）、ETH3D（点图）、ScanNet-1500（匹配）、TAP-Vid（跟踪）。

---

## 5. 执行流程

```
① 生成预测结果
   方式 A：跑 demo_gradio_cn.py，上传图片/视频后点 Reconstruct
           → 自动产出 input_images_<时间戳>/predictions.npz
           → 并自动备份到 vggt_output/gradio/<时间戳>/
   方式 B：写批量脚本，按测试用例循环调用 run_model()，逐例落盘 npz

② 计算自洽性指标（离线，无需 GPU）
   python metrics.py "vggt_output/gradio/*/predictions.npz" \
          --conf-thres 3.0 --pairs 30 --json vggt_output/metrics.json

③ 汇总
   从 JSON 抽取 G1–G4 数值填表，画帧数/扰动强度的退化曲线
```

**注意**：G4（光度一致性）与 G5（分段一致性）的绝对值**不能**用统一阈值判定，原因有三 —— 遮挡、视角相关光照（高光/阴影）、深度不连续处的重采样。它们的正确用法是**同一场景下的横向对比**（不同帧数、不同扰动量级之间比），而不是跨场景比绝对值。

---

## 6. 结果记录模板

| 用例 | 场景 | 帧数 | G1 mean_L2 | G1 相对 | G2 像素误差 | G2 深度相对误差 | G3 低置信占比 | G4 L1 | G4 NCC | G4 有效率 | 耗时(s) | 峰值显存(GB) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-01 | room | 8 | | | | | | | | | | |
| TC-02 | llff_fern | 20 | | | | | | | | | | |
| TC-03 | kitchen | 25 | | | | | | | | | | |
| TC-05 | room | 1 | | | | | | | — | — | | |

---

## 7. 判定标准（建议）

| 指标 | 通过准则 | 说明 |
|---|---|---|
| G1 相对误差 | < 1% 场景尺度 | 三个 head 一致 |
| G2 像素误差均值 | < 1 px | 点图与相机头自洽；超过说明外参/内参有问题 |
| G2 深度相对误差均值 | < 2% | |
| G3 低置信占比 | < 20% | 过高表示场景超出模型能力 |
| G4 NCC | > 0.9 | 绝对阈值仅作粗筛，主要看趋势 |
| G4 有效投影率 | > 50% | 过低说明遮挡严重或视差估计失败 |
| 确定性 | 同 seed 两次运行四指标**完全一致** | |
| 异常输入 | 不崩溃，给出可读报错 | |
| 连续 10 次运行 | 峰值显存无单调增长 | 无内存泄漏 |

> 上述阈值是**初值**，应在 TC-01 拿到基线后据实测分布调整。首次跑完请把基线值回填到本表。

---

## 附录 A：`metrics.py` 用法

```bash
# 单文件
python metrics.py vggt_output/gradio/20260910_101336_123456/predictions.npz

# 指定置信度阈值与光度一致性的帧对数，并导出 JSON
python metrics.py predictions.npz --conf-thres 3.0 --pairs 30 --json vggt_output/metrics.json

# 批量处理所有历史结果
python metrics.py "vggt_output/gradio/*/predictions.npz" --json vggt_output/metrics_all.json
```

输出四组指标：`pointmap_vs_depth`（G1）、`pointmap_vs_camera`（G2）、`confidence`（G3）、`photometric`（G4）。任一指标缺数据时会标记为不可用并给出原因，不会中断。

**已验证**：用几何严格自洽的合成场景（6 帧，相机环绕平面，图像由世界坐标纹理渲染）做回归测试，7 组用例全部通过 ——
- 完美数据下 G1 `mean_l2 = 0.0`、G2 像素误差 `0.0000 px`、G4 NCC `1.0000`；
- 点图头注入 σ=0.02 噪声后，G1 升至 0.0316、G2 升至 0.66 px，而 G4 不受影响（0.0048）；
- 相机位姿俯仰 3° 后，G2 升至 **3.608 px**，与理论值 `f·tan(3°) ≈ 3.5 px` 吻合，G4 由 0.0048 升至 0.0294；
- 缺字段、单帧、阈值过滤殆尽、通配符批量等边界情况均优雅降级。

---

## 附录 B：为什么环闭合检验对 VGGT 无效（推导）

设相机 i 的 `extrinsic` 为 `T_i`（world→cam，4×4 齐次形式），则从帧 A 到帧 B 的相对变换为

```
T_{A→B} = T_B · T_A⁻¹
```

对 A→B→C→A 的闭合回路，复合变换（矩阵乘法顺序与施加顺序相反）为

```
T_{C→A} · T_{B→C} · T_{A→B}
= (T_A T_C⁻¹)(T_C T_B⁻¹)(T_B T_A⁻¹)
= T_A (T_C⁻¹ T_C)(T_B⁻¹ T_B) T_A⁻¹
= T_A T_A⁻¹
= I
```

中间项两两抵消，**结果与 `T_A`、`T_B`、`T_C` 的具体取值无关**。只要三帧位姿来自同一次预测（互相自洽），无论预测多离谱，环合残差都只有浮点误差。

这与 SfM 场景不同：传统 SfM 中每对相对位姿是**独立估计**的（不同图像对分别做匹配与求解），所以环合残差能真实反映累积漂移。

**可行的替代 —— G5 分段一致性**：

1. 用帧 `[0 … N1]` 跑一次 → 位姿集合 `P_A`
2. 用帧 `[k … N-1]`（与前者重叠 `[k … N1]`）跑一次 → 位姿集合 `P_B`
3. 对重叠帧 `i, j`，在两套结果里各算**相对**位姿 `T_{i→j}`（相对量对坐标系选择不变）
4. 比较旋转角误差（°）与平移误差（按场景尺度归一）

此时两次预测的输入窗口不同、锚点不同，比较才真正有意义 —— 它量化的是"模型输出对上下文窗口的依赖程度"。
