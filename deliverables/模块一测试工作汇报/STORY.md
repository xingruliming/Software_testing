# STORY.md — 模块一测试工作汇报

**时长**：约 6 分钟 · 14 页
**受众**：课程指导教师 + 同组同学
**唯一主张**：我们测的不是普通确定性代码，而是**大模型内部唯一能脱离权重单测的确定性几何层**。全篇按老师四点要求组织：**被测对象核心功能（P03–05）/ 主要测试策略（P06–09）/ 自动化设计与实施（P10）/ 测试结果与结论（P11–13）**；方法上以**等价类 + 边界值**两种经典方法为主力（满足「≥2 种」），所有标注与用例清单逐字一致、逐条可对账。

---

## 叙事主线

一条因果链贯穿全篇：
**被测对象特殊（大模型里的确定性孤岛）→ 所以断言方式必须特殊（只能比相对量/解析解）→ 所以方法必须特殊（两种经典方法打底 + 三类加固标注）→ 所以实施必须特殊（一条命令、双产出、逐位一致）→ 结果与结论是这套设计的自然产物。**

不做"我们用了等价类/边界值"这类泛泛罗列；每一处都给出**可对账的用例号**与**具体的对比对象**。

## 节奏（14 页，对应老师四点要求）

| 页 | 章节 | 时长 | 目的 |
|---|---|---|---|
| 1 | 封面 | 15s | 定调：32 条用例 / 2 种方法 / 0 权重 0 GPU |
| 2 | 目录 | 10s | 四条 = 老师四点要求（被测对象 / 测试策略 / 自动化实施 / 结果与结论） |
| 3 | ①被测对象 | — | 章节扉页 01：被测对象核心功能 |
| 4 | ①被测对象 | 40s | 被测对象（唯一可单测的一层）+ 核心功能链 + 测试要点 |
| 5 | ①被测对象 | 40s | 文档承诺与实现不一致 → 决定断言策略 |
| 6 | ②测试策略 | — | 章节扉页 02：主要测试策略 |
| 7 | ②测试策略 | 40s | **两种经典方法 + 三类加固**（等价类 19 / 边界值 5 / 组合覆盖 4 / 错误推测 2 / 判定表 2） |
| 8 | ②测试策略 | 45s | **用例 → 方法映射表（逐条对账）** |
| 9 | ②测试策略 | 40s | 形状矩阵：用最小输入把两类失败分开 |
| 10 | ③自动化 | 40s | **自动化设计与实施**：8 文件一键运行 / 双产出 / 逐位一致 |
| 11 | ④结果与结论 | 25s | 33 执行 / 31 通过 / 2 失败 / 93.9% 的一手数据 |
| 12 | ④结果与结论 | 35s | DEF-M1-001 的最小输入与调用栈定位 |
| 13 | ④结果与结论 | 30s | **四条结论**：质量 / 缺陷处置 / 方法 / 工程 |
| 14 | 收尾 | 20s | 三张牌 + 收束 |

## 老师要求的落点

> 要说明**被测对象核心功能、主要测试策略、自动化测试设计与实施、测试结论**等。

| 老师要求 | 对应页 | 落点 |
|---|---|---|
| 被测对象核心功能 | P03–P05 | VGGT：Meta AI 与牛津大学的前馈式三维视觉大模型、CVPR 2025 最佳论文，约 10 亿参数，单次前向即可从一至数百张图像预测相机内外参/深度图/点图/三维轨迹；被测模块 geometry.py 324 行 8 个函数（NumPy/PyTorch 混合），深度图↔相机↔世界↔像素转换链（unproject L15 → depth_to_world L47 → depth_to_cam L87）；规范自由度与文档契约 |
| 主要测试策略 | P06–P09 | 等价类划分 + 边界值分析（满足 ≥2 种）；组合覆盖 / 错误推测 / 判定表加固；用例→方法逐条对账；形状矩阵最小样例 |
| 自动化测试设计与实施 | P10–P11 | tests/module1_coordinate/ 8 个用例文件；run_tests.ps1 一键运行；junit_report.py 双产出（JUnit XML + UTF-8 日志）；退出码 0/1/2/3；单次 ≈0.02s；33 执行 / 31 通过 |
| 测试结论 | P12–P13 | 唯一缺陷 DEF-M1-001（L39 无条件 squeeze(-1)）；质量 / 处置 / 方法 / 工程四条结论 |

### 方法标注口径（≥2 种方法）

**等价类 + 边界值两种方法打底，已满足「≥2 种」**；PPT 上每一个方法名、每一条计数都**与用例清单备注列逐字一致**：

| 清单标注 | 条数 | 依据用例（与清单备注列一致） |
|---|---|---|
| 等价类划分 | 19 | 001/004/006/007/008/009/011/012/013/016/017/018/020/021/023/024/026/027/030 |
| 边界值分析 | 5 | 002 图像四角 · 003 深度下界 0 · 010 矩阵尺寸 · 014 eps−/eps/eps+ · 025 Z=0 除零 |
| 组合覆盖 | 4 | 015 深度×旋转 · 022 帧维广播 · 028 平移×内参 · 031 批量内参 |
| 错误推测 | 2 | 005 非零 skew · 019 批量长度不一致 |
| 判定表 | 2 | 029 only_points_cam 分支 · 032 extra_params 有/无 |

⚠️ **场景法不采用**：清单备注列里**没有"场景法"标注**，PPT 不做无依据声明（曾写入后撤下）。
若老师追问"为什么没用场景法"：等价类 + 边界值已满足 ≥2 种；027/028（端到端投影）、
012/013（相机↔世界往返）等用例确有调用链场景形态，但清单标注是等价类/组合覆盖，不另造标签。

## 关键修辞

- **对比法**：把"常规做法"与"我们的做法"并置。
- **可对账**：每条方法都给出 `M1-GEO-xxx` 用例号，不空谈方法名。
- **具体数字**：324 行 / 8 函数 / 5.0 GB 权重 / 32 条用例 / 0.02 秒 / 三层调用栈。
- **代码坐标**：`L15 → L47 → L87` 调用链；`L22` 文档契约；`L39` squeeze 根因；`L98` 解包报错点。

## 反例（明确不写）

- ✗ "测试非常重要，质量是生命线" —— 无信息量的口号。
- ✗ 任何未经我们实测的第三方数据。
- ✗ B 线（图像处理）内容 —— 用户明确要求忽略。
- ✗ 清单里不存在的标签（如"场景法"）—— PPT 不做无依据声明。

## 事实基线（全部来自本仓库实测）

- **VGGT（官方口径，来源：CVPR 2025 论文 / HF `facebook/VGGT-1B` / vgg-t.github.io）**：
  Meta AI Research + 牛津大学 VGG 的前馈式三维视觉大模型，**CVPR 2025 最佳论文**；
  单次前向直接推断场景全部关键 3D 属性——相机内外参（OpenCV 约定）、深度图、点图、
  3D 点轨迹，输入 1 到数百张视图，无需 bundle adjustment / 全局对齐后处理；
  发布模型 **VGGT-1B ≈ 10 亿参数**（架构约 1.2B），权重文件 5.0 GB。
- 被测模块 `vggt-main/vggt/utils/geometry.py`：324 行、8 个函数，
  **NumPy/PyTorch 混合实现**（`import torch`；深度转换链为 NumPy，
  project/img_from_cam/cam_from_img 等为 torch）——**任何材料不得写「纯 NumPy」**。
- **行号以源码为准**：`unproject_depth_map_to_point_map` L15（docstring L22、squeeze L39）、
  `depth_to_world_coords_points` L47、`depth_to_cam_coords_points` L87（`H, W` 解包 L98，即报错点）、
  `closed_form_inverse_se3` L120、`project_world_points_to_camera_points_batch` L175、
  `project_world_points_to_cam` L204、`img_from_cam` L251、`cam_from_img` L294。
- 权重 `E:\0_work\1shijian\model.pt`：约 5.0 GB；本模块测试**不需要**它。
- 自动化：`tests/module1_coordinate/` 8 个用例文件（M1-GEO-001~032 + 019 绕障对照）；
  `run_tests.ps1`（PYTHONPATH 注入 vggt-main，不改基线）；`tests/junit_report.py`（stdlib，
  一次运行产 JUnit XML + UTF-8 日志）；退出码 0/1/2/3。
- 执行：`tests=33 failures=0 errors=2 skipped=0`，通过率 93.9%，单次 ≈0.014–0.02 s。
- 清单 Information 页统计：32 条 = 30 OK + 2 NG（OK 率 93.75%）；33 口径含清单外的对照用例。
- 失败：`M1-GEO-016`、`M1-GEO-018`（同源，`DEF-M1-001`）。
- 规范自由度：第一帧 `extrinsic` 恒为单位阵（场景参考系）。
- 实证探针结论：用例清单中 **3 条**预期结果与真实实现不符，已据实调整。
