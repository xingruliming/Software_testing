# 模块一交付件说明

本目录存放模块一（算法坐标转换系统，A 线）的课程交付件。

## 文件清单

| 文件 | 用途 | 状态 |
|---|---|---|
| `VGGT模块一测试用例清单.xlsx` | 附录1 填报版。31 行正式用例（`M1-GEO-001`~`032`，含 1 条对照共 33 条执行） | 已回填真实执行结果与状态 |
| `VGGT模块一缺陷报告.docx` | 附录2 缺陷报告，当前登记 `DEF-M1-001` | 已填写复现与定位；修复/复测栏待开发完成后补 |
| `测试操作手册.docx` | 环境准备、一键运行、证据归档与附录填报说明 | 已更新至当前实现状态 |
| `交付检查清单.md` | 提交前逐项核对 | 已完成核对 |

## 当前执行事实（2026-09-14）

- 用例清单 `Result 实际结果` 列逐条写明实测观测值；`Status 是否通过`列为
  **30 条 `OK` + 2 条 `NG`**（`M1-GEO-016`、`M1-GEO-018`）。
- 依据 `unittest -v` 实测：**33 条执行（含 019 对照）/ 31 通过 / 2 失败**，通过率 93.9%。
- 2 条失败同源，均指向**尚未修复**的 `DEF-M1-001`（`geometry.py:98` 解包失败）。
- 按模板定义：`OK`=通过、`POK`=部分通过、`NG`=不能通过、`NT`=用例本身无法执行。
  本批没有「部分通过」，也没有「用例写错」，故 `POK`/`NT` 不出现。

## 待人工补全

模板中的姓名、学号、日期、贡献比例，以及 `DEF-M1-001` 的解决/复测字段，
仍需由小组成员据实填写。
